## 충돌사례

### case#1
원격레포와 로컬 레포의 커밋 이력이 같은 상황에서 로컬에서
working directory에서의 수정 이후 git pull 한다면

- 에러 발생하지 않음

```bash
# pull = fetch + merge
# fetch는 성공인데 merge가 실패한 경우
git pull origin main
```
충돌이 아니므로 그냥 git push 하면됨

### case#2
git push
원격에 있는 소스만 받아옴


### case#3

### case#4

### case#5

